// pipeline is what binds together our actions and transformations
package pipeline

// We need an order of operations
// We need instructions for each as well, so this will likely need to be a struct to handle the instructions

type Pipeline struct {
}
